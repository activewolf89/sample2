import React from 'react';
import Div from 'shared/Div';

// -----
const Q4 = (props) =>{
  return (
    <Div >
      Re-creating pieces is something we can do, with the caveat that any designer piece cannot be done exactly as this is a look that cannot be replicated.  It is best to send us a picture of the piece via custom-job form, and we will let you know what can be done.
    </Div>
  )
}
Q4.propTypes = {
}
export default Q4;
