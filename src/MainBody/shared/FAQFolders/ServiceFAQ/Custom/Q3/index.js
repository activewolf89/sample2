import React from 'react';
import Div from 'shared/Div';

// -----
const Q3 = (props) =>{
  return (
    <Div >
      An example of this process can be creating a fashion ring with an existing stone.  We would listen carefully to the requests, create a sketch and eventual wax of the piece.  This is a hand-carved wax typically or can be done with a molding of the ring if previously done before.  After this process and approval, we cast in whatever metal we would like, polish/finish coming back from our casters, set the center-stone and write up the appraisal. 
    </Div>
  )
}
Q3.propTypes = {
}
export default Q3;
