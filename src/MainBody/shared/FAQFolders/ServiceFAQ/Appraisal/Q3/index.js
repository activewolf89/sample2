import React from 'react';
import Div from 'shared/Div';

// -----
const Q3 = (props) =>{
  return (
    <Div >
      The value placed on the appraisal is typically more than the actual cost of the piece.  The reason for this is the fact that if the components of the jewelry goes up in value, it is better overtime to have a buffer for this situation.  In the event a piece of lost as well, it is always better to get a little bit more than the actual piece itself as well.
    </Div>
  )
}
Q3.propTypes = {
}
export default Q3;
