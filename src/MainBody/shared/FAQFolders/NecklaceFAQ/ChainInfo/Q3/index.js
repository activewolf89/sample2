import React from 'react';
import Div from 'shared/Div';


// -----
const Q3 = (props) =>{
  return (
    <Div >
      An extra loop can be added within the chain to length or shorten the chain depending on the look the wearer is trying to achieve, and can be applied to all precious-metal chains where one can solder a loop onto.
    </Div>
  )
}
Q3.propTypes = {
}
export default Q3;
